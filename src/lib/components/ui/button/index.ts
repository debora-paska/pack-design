import Root, {
	type ButtonIconName,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants
} from './button.svelte';

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	buttonVariants,
	type ButtonIconName,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
};
